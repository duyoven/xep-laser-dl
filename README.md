# Xep Laser — Duy's Oven (bản tải)

Kho công khai chỉ để **tải file cài + tự động cập nhật** cho app Xếp Laser.
Mã nguồn nằm ở kho riêng. File mới nhất xem ở mục **Releases**.

- `version.json` — phiên bản mới nhất (app đọc file này để tự cập nhật).
